[plugin]

name="ArUco Android Export Guard"
description="Fails Android exports when the libopencv_aruco GDExtension .so files for the enabled ABIs are missing, instead of silently shipping a broken APK."
author="Dennis"
version="1.0"
script="aruco_export_guard.gd"
